from setuptools import setup, find_packages

setup(
    name="anyoneai-project",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # requirements will be read from requirements.txt
    ],
) 